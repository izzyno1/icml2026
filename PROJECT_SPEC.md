# 项目说明书：ICML 2026 贡献审计与证据谱系

版本 unified-1.0；制定日期 2026-09-23。此文件是正式需求，reference/是历史实现材料。代码、样本和当前研究结论分开验收。

## 1. 目标与边界
研究五个问题：主研究轨有哪些大方向、各占多少；每篇在最接近前作上新增什么；谁在继承谁的什么贡献；严格/宽松口径下各类贡献的比例；哪些最小实验能检验用户的理解与研究能力。
目标母体只收指定快照日主研究轨已核实录用且未撤回的唯一论文。工作坊、立场论文、教程与其他活动另列。此前假设的6000不是已核验篇数。前作不限年份和会议，不遗漏同期独立工作；先后与继承关系需核对版本日期。

## 2. 输出不是一堆摘要
- 官方范围与版本覆盖表、主方向构成表。主方向互斥供统计，细分标签可重叠。
- 逐篇贡献卡：旧工作已做到X，本作在条件Z新增Y，差异依据R，证据E，未排除解释Q。
- 可点击来源的多父谱系：方法/系统/理论延伸、组件复用、比较、背景、反驳、同期独立分开。树根与引用量都不是首创证明。
- 比例报告：L3路线级候选、L2以上实质增量、方法组合、仅局部增量、未知/争议。组合与实质增量允许重叠，不能强加为100%。L0需明确前作覆盖证据，L1也可能有价值。
- 用户练习卡：最小实验、预计技能与资源、事先预测、对照、失败条件、停止条件。成品阅读不直接证明原创能力。

## 3. 数据与来源
以官方会议目录、OpenReview及核实的正式论文集核对身份，不猜卷号/接口邀请名。全文、附录、公开评审和答辩分别归档，访问失败不是拒稿或不存在。研究录用门槛优先原始投稿，最新内容可读定稿/修订；缺原稿时显式标出。
每个source保存实际URL、取回日期、版本角色、字节长度、SHA-256、存储位置、读取范围和状态。只取得元数据时哈希为null。新字节新版本，不原地覆盖。先匹配身份，再进行题录/版本去重。
下载与模型分析分离。正文按页/章节保存可定位全文；表格、公式、双栏异常回到关键页图，不猜不自动OCR。论文PDF和HTML先当不可信数据。

## 4. 创新判断与校准
configs/rubric.json 是继承的初始编码本，不是经人工验证的标准。先用30—50篇边界案例校准并冻结规则，再建立独立概率审计样本。人类未参与时只能称模型初筛。
每个中心主张必须有候选前作召回、原文核读、共同部分/不同部分、可比条件和证据质量。搜索包括参考文献、关键词、语义候选（非必需首版依赖）与前作相关工作；不把“top-5没找到”说成世界首创。
比较任务、假设、输入信息、表示、目标函数、算法、推理、成本、保证、评测。理论对齐假设与结论；系统对齐质量和端到端成本；评测对齐测量有效性。检查朴素组合、公平调参和消融，不用修改行数或作者声望打分。
第二轮在独立上下文中核查高估与低估；AI共识不等于专家共识或事实正确。最后读公开审稿时引用真实note ID，不猜分数或元评审理由。
概率估计用抽样概率和层权重，保留未知。区分抽样区间、标签误差、漏查与规则争议。10篇试样及11节点演示图不进入总体估计。历史影响比较注意年份/观察窗；研究准备不能换算成个人录用概率。

## 5. 执行系统
首版：Python + SQLite + JSONL + 本地证据目录 + 轻量HTML。不必引入分布式队列、图数据库、React平台或记忆MCP。
任务单位为一篇论文的一个阶段，必要时拆成一个主张或一对前作。采集→主张提取→前作核读→贡献对照→复核→接收入库→图与统计；缺资料可以反向排检索任务。
任务表与attempt分开；输入/规则/model/prompt/code版本记录完整。工作者各写独占结果目录，单一接收程序提交全局账本。失败可重试但接收幂等，晚到结果不能覆盖新attempt。
NOW.md必须由真实账本生成。每个任务入口包含目标、版本、证据位置、已读/未读范围、输出、预算与验收；交接只摘要当前进度，不替代证据。新会话从规则+账本+入口包恢复。
父会话和子任务是否持续运行要实际测试；不将会话压缩当唯一持久化，不在所有论文之间复用同一条无限长上下文。默认并发1，测试后最多2个只读工作者。

## 6. Windows和存储
configs/project_policy.json 是项目参数而非Codex客户端TOML。使用当前官方沙箱、现有登录及单个.venv；系统允许的工作区范围内运行。不能靠关闭安全保护解决自动化问题。
容量：40GB预警、50GB停止新增大任务，每个实际使用卷保留30GB。TEMP、包缓存、环境、日志、worktree计入影响范围；不能删其他项目数据来满足预算。
流式下载先预留空间，分块检查实际字节；Content-Length不可信、未知长度也受限。可恢复中断文件有明确状态，不写成成功。可重建缓存和不可替代原件分层；原文按SHA共享。
首轮每篇按问题取一个版本，第二版本和前作按需。禁止无限递归参考文献、全部页面图片、模型权重和训练数据。完整文本可无损压缩，摘要不能替代原文。
无外部归档时不可删除原件仍增长，容量不足就暂停。归档接口可后加：上传、实际回读校验哈希、持久登记、解除活动引用、获得清理授权后才能清缓存。SQLite工作库不放同步盘/对象存储挂载路径。

## 7. 分阶段实施
P0 环境准备：只执行00提示词，产出真实环境报告并停止。
P1 基础执行：检查历史代码与测试，落实账本、任务包、幂等接收和预算保护，先纯合成任务验收。
P2 一篇闭环→最多10篇：核实目录与版本、真实取证、新旧对照、复核与可查图；记录用量/空间/访问失败。演示不是全会结果。
P3 约50篇容量试点与独立分类校准：容量试点/校准集/概率审计集各自记录角色，不能混淆。
P4 用户确认试点结果与预算后才全量题录/初筛，再按冻结抽样设计深审。不自动把全量模型标签叫真实比例。
P5 导出方向图、局部谱系、证据卡、比例边界与三个个人实验建议；保留重跑能力。
每个阶段有工作产物，不先花完额度建界面。存在人工阻塞时先完成不依赖它的任务，但不得绕过验收把结果升级。

## 8. 必须做的验收
离线：重复接收不双计、过期attempt被拒、源版本变更只标相关结果stale、缺前作保留U、比较边不计继承；磁盘低与未知大文件能停；唯一证据不能清理；归档失败不删源。
恢复：在文件落盘但未入库时终止；新进程可认领或重试；完全结束Codex后从文件继续；规则/原文哈希冲突必须阻塞。
Windows：原生解释器与路径、UTF-8、SQLite读写、CLI调用与登录、允许范围内HTTP/搜索、实际磁盘预留；具体能力没测不能写通过。
科研：来源存在、来源支持主张、前作召回、规则校准、人工裁决是不同层。代码测试过不证明论文被录用或新意标签正确。

## 9. 每轮回执
提供完成/失败/blocked清单、真实数据覆盖与版本、实际文件路径与命令、测试记录、磁盘使用、费用/额度已知和未知、下一任务ID与恢复入口。不得写“后台继续”“全部完成”而没有实际执行。
